name: chatbot
root: ~/work/chatbot/
windows:
  - editor:
      - git pull
  - servers:
      layout: tiled
      panes:
        - conversation:
            - cd packages/conversation
            - pnpm build:deps
            - pnpm dev
        - admin:
            - cd packages/admin
            - pnpm build:deps
            - pnpm dev
        -  # regular zsh shell
